version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: uv
    directory: /
    schedule:
      interval: weekly
    ignore:
      # Pinned in pyproject.toml (see the pin's comment) until py-pdf/pypdf#4067 is fixed.
      - dependency-name: "pypdf"
