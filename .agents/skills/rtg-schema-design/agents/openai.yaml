interface:
  display_name: "RTG Schema Design"
  short_description: "Design precise RTG concepts and graph schemas"
  default_prompt: "Use $rtg-schema-design to classify these graph concepts and propose the smallest precise RTG schema."
policy:
  allow_implicit_invocation: true
