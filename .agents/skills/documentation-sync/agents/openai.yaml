interface:
  display_name: "Documentation Sync"
  short_description: "Align repository guidance with current authority"
  default_prompt: "Use $documentation-sync to align the repository documentation, commands, skills, and public status."
policy:
  allow_implicit_invocation: true
