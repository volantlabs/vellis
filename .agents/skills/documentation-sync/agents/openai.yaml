interface:
  display_name: "Documentation Sync"
  short_description: "Keep repository docs aligned"
  default_prompt: "Use $documentation-sync to update Vellis documentation after specs, code, tooling, skills, or workflow rules change."
policy:
  allow_implicit_invocation: true
