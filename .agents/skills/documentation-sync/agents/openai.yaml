interface:
  display_name: "Documentation Sync"
  short_description: "Align guidance with current model authority"
  default_prompt: "Use $documentation-sync to align affected guidance and metadata while distinguishing modeled, selected, implemented, and runnable claims."
policy:
  allow_implicit_invocation: true
