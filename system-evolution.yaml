schema_version: "1.0"
evolution:
  id: vellis-2-distribution-install-fix
  objective: Give Vellis a working, guarded git-hosted install path so the documented onboarding command actually installs the tool and stays truthful over time.
  observable_distinction: The documented `uv tool install git+...` command installs and runs correctly, an automated check derived from README.md fails when that command or its required Install heading drifts, and package_smoke's expected version no longer risks silent drift from pyproject.toml.
  lifecycle: active
  approval:
    status: not-required
    reason: model/50-verification.sysml already requires proving the MCP boundary from installed wheel and source distribution; this closes an evidence and realization gap under that existing acceptance, and the owner selected the git+ distribution channel directly.
    checkpoint: null
  blocker: null
  checkpoint: git:1e5f11230e8423c1631f8628cbc068cce03a19a2
baselines:
  status: current
  source: &source
    model: sha256:7fcb28cb48bc81589d6c17ffc5ade6421d7c5809964f28302f631c0de2d249bc
    implementation: git:7b32a6f7661048a57d0a91fea2580b4facbefd92
    language: sha256:054aab62276a55c43860e3d2daad5fae864db550b3a58ab110074f8b293996ff
    execution_environment: sha256:a486c22174806217cbf30c4c5b2a088421e4e9c5633fd507506e3711c88b57b8
    checkpoint: git:7b32a6f7661048a57d0a91fea2580b4facbefd92
  target: &target
    model: sha256:7fcb28cb48bc81589d6c17ffc5ade6421d7c5809964f28302f631c0de2d249bc
    implementation: git:1e5f11230e8423c1631f8628cbc068cce03a19a2
    language: sha256:054aab62276a55c43860e3d2daad5fae864db550b3a58ab110074f8b293996ff
    execution_environment: sha256:a486c22174806217cbf30c4c5b2a088421e4e9c5633fd507506e3711c88b57b8
    checkpoint: git:1e5f11230e8423c1631f8628cbc068cce03a19a2
  observed: *target
scope:
  authority_scope:
    - VellisRequirements::simpleOperation
    - VellisVerification::mcpLifecycle
  implementation_scope:
    - system-evolution.yaml
    - README.md
    - justfile
    - pyproject.toml
    - tools/package_smoke.py
    - tests/test_repository_policy.py
    - vellis/__main__.py
    - docs/install.md
    - AGENTS.md
    - CONTRIBUTING.md
    - .github/workflows/check.yml
  review_lenses: [authority and conformance, engineering and evidence]
  non_goals:
    - No model authority, public MCP tool, wire shape, database schema, or database version change.
    - No PyPI publish or GitHub Release artifact channel; the owner selected git+ install only.
    - No change to VEL001/VEL002's initialization-mode requirement; only its CLI error text improved. The --connect/--transport noninteractive messages instead realize VellisRequirements::simpleOperation, not VEL001/VEL002.
findings:
  - id: F001
    summary: The only documented install command referenced a wheel that no repository command produced, and no check ever ran it.
    classification: implementation defect
    consequence: A person or agent following README.md could not install Vellis at all, and the installed-boundary proof VellisVerification::mcpLifecycle requires was only exercised through package_smoke's own ad hoc build, never through the publicly documented command.
    authority_refs: [VellisRequirements::simpleOperation, VellisVerification::mcpLifecycle]
    evidence_refs: [path:tests/test_repository_policy.py#test_documented_subcommands_match_the_owner_command, path:README.md#install, path:docs/install.md#troubleshooting]
    disposition: resolved
    owner_work_item_id: W001
    implementation_status: conforming
    nearest_wrong_system: A README install command that references a nonexistent artifact, or a regression guard that hardcodes its own copy of that command instead of parsing README.md, would both silently diverge from what a user actually runs.
    compatibility_effect: No public tool, wire shape, or CLI subcommand changes; only install documentation and build tooling change.
  - id: F002
    summary: package_smoke.py held its own unguarded VERSION literal instead of reading pyproject.toml.
    classification: implementation defect
    consequence: A version bump that updated pyproject.toml, vellis/__init__.py, and the policy test literal could still leave package_smoke silently smoke-testing under a stale expected version.
    authority_refs: [VellisVerification::mcpLifecycle]
    evidence_refs: [command:just package-check]
    disposition: resolved
    owner_work_item_id: W001
    implementation_status: conforming
    nearest_wrong_system: A wheel built at a new version still passes package_smoke's installed-import assertion because its expected VERSION literal was never bumped alongside it.
    compatibility_effect: No product effect; only the smoke test's own version comparison source changes.
decisions:
  - id: D001
    summary: Derive the tested install command from README.md's own Install section rather than duplicating it in package_smoke.py.
    authority_refs: [VellisVerification::mcpLifecycle]
    alternatives: [Hardcode a second copy of the install command in the smoke test., Add a separate install-check tool and just recipe., Trust the README without any automated check.]
    reversible: true
    owner_work_item_id: W001
    evidence_intent: [A renamed or missing Install heading fails the check., A command that no longer names the canonical repository URL fails the check., A genuinely broken install flag fails the check., The real UV_TOOL_DIR and UV_TOOL_BIN_DIR remain untouched by the check.]
    implementation_status: conforming
    evidence_refs: [command:just package-check]
  - id: D002
    summary: Read package_smoke's expected VERSION from pyproject.toml via tomllib instead of a hardcoded literal.
    authority_refs: [VellisVerification::mcpLifecycle]
    alternatives: [Import vellis.__version__ directly., Leave the literal duplicated and rely on manual discipline.]
    reversible: true
    owner_work_item_id: W001
    evidence_intent: [package_smoke's expected version always matches pyproject.toml without a separate edit.]
    implementation_status: conforming
    evidence_refs: [command:just package-check]
work_items:
  - id: W001
    order: 1
    label: Fix and guard the documented git+ install path
    kind: implementation
    dependencies: []
    planned_baseline: {dimension: implementation, identity: "git:7b32a6f7661048a57d0a91fea2580b4facbefd92"}
    finding_ids: [F001, F002]
    decision_ids: [D001, D002]
    authority:
      - refs: [VellisRequirements::simpleOperation, VellisVerification::mcpLifecycle]
        coverage: partial
        remaining_work_item_ids: [W002]
    approval:
      status: not-required
      reason: This work closes an evidence and realization gap under sufficient accepted authority; the owner directed the git+-only distribution channel.
      checkpoint: null
    nearest_wrong_system: The documented install command appears correct in isolation while package_smoke never actually exercises it, or a version bump silently leaves the smoke test comparing against a stale expected version.
    compatibility_effect: No public tool, wire shape, database schema, or CLI subcommand changes; install documentation, build tooling, packaging metadata, CI, and two CLI error messages change.
    non_effects: [Do not publish to PyPI or attach release artifacts., Do not change VEL001/VEL002's initialization-mode requirement., Do not mutate external client configuration during tests.]
    lifecycle: complete
    implementation_status: conforming
    evidence_refs: [command:just package-check, command:just check, path:tests/test_repository_policy.py#test_documented_subcommands_match_the_owner_command]
    blocker: null
    checkpoint: git:1e5f11230e8423c1631f8628cbc068cce03a19a2
  - id: W002
    order: 2
    label: Verify, independently review, and close the install-path fix
    kind: closure
    dependencies: [W001]
    planned_baseline: {dimension: implementation, identity: "git:1e5f11230e8423c1631f8628cbc068cce03a19a2"}
    finding_ids: []
    decision_ids: []
    authority:
      - refs: [VellisRequirements::simpleOperation, VellisVerification::mcpLifecycle]
        coverage: full
        remaining_work_item_ids: []
    approval:
      status: not-required
      reason: Closure verifies existing accepted meaning and applies deterministic bookkeeping.
      checkpoint: null
    nearest_wrong_system: Individual fixes look correct while the documented command still fails end-to-end, or public claims in README.md, docs/install.md, and CI diverge from actual behavior.
    compatibility_effect: No additional compatibility effect beyond W001.
    non_effects: [Do not open a model-authority change., Do not publish to PyPI.]
    lifecycle: active
    implementation_status: conforming
    evidence_refs: [command:just package-check, command:just check]
    blocker: null
    checkpoint: null
closure:
  finding_disposition: complete
  model_status: unchanged
  implementation_status: conforming
  integration_status: conforming
  external_status: conforming
  compatibility: No public tool, wire shape, database schema, or CLI subcommand changes. The documented install command, README, docs/install.md, AGENTS.md, CONTRIBUTING.md, CI, and two CLI error messages change; the owner-selected distribution channel is git+ install only, with no PyPI or release-artifact publication.
  reviews:
    - {lens: authority and conformance, status: pending, reviewer: null, checkpoint: null, evidence_refs: []}
    - {lens: engineering and evidence, status: pending, reviewer: null, checkpoint: null, evidence_refs: []}
  evidence_refs: [command:just package-check, command:just check]
  checkpoint: null
