---
specification_id: "kerml-1.0"
specification: "Kernel Modeling Language (KerML) Version 1.0"
document_number: "formal/2026-03-01"
source_url: "https://www.omg.org/spec/KerML/1.0/PDF"
source_sha256: "3bcc96f989bfa9d05cd28e026df3351b795fe8d494187b87bff3db7d96373697"
physical_page: 97
printed_page: "71"
generated: true
section_context_before_page:
  - "7 Language Description"
  - "7.4 Kernel"
  - "7.4.13 Metadata"
  - "User-Defined Keywords"
section_paths_starting_here:
  - ["7 Language Description", "7.4 Kernel", "7.4.14 Packages"]
sections_starting_here:
  - "7.4.14 Packages"
extraction_warnings: []
---

# kerml-1.0 physical page 97 (printed 71)

## Extracted specification text

7.4.14   Packages

Metamodel references:

       •  Concrete syntax,    8.2.5.13
       •  Abstract syntax,   8.3.4.13
       •  Semantics,   8.4.4.14

Packages  are namespaces used to group elements, without any instance-level semantics (as opposed to types, which
are namespaces with classification semantics, see    7.3.2 ). A package is notated like a generic namespace (see
7.2.5.2 ), but using the keyword  package    instead of namespace.

        package    AddressBooks {
             datatype     Entry {
                   feature     name[1]: String;
                   feature     address[1]: String;
             }
             struct    AddressBook {
                   composite      feature    entries[*]: Entry;
             }
        }

A package may also have one or more      filter conditions for selecting a subset of its imported memberships. A filter
condition is a Boolean-valued, model-level evaluable expression (see      7.4.9 ) that must evaluate to true for any
imported member of the package. These are notated using the keyword         filter    followed by the filter condition
expression.

        package    Annotations {
             metaclass      ApprovalAnnotation {
                   feature     approved[1] : Boolean;
                   feature     approver[1] : String;
                   feature     level[1] : Natural;
             }
             ...
        }

        package    DesignModel {
             public import        Annotations::*;
             struct    System {
                     @ApprovalAnnotation {
                         approved = true;
                         approver = "John Smith";
                         level = 2;
                   }
             }
             ...
        }

        package    UpperLevelApprovals {
             // This package imports all direct or indirect members
             // of the DesignModel package that have been approved
             // at a level greater than 1.
             public import        DesignModel::**;
             filter    @Annotations::ApprovalAnnotation                and
                  Annotations::ApprovalAnnotation::approved                   and
                  Annotations::ApprovalAnnotation::level > 1;
        }

A filter condition can operate on  metadata on elements (see    7.4.13 ), such as checking for a metadata feature of
aparticular type or accessing the values of the features of a metadata feature. For the purposes of filter condition

---

Source: [Kernel Modeling Language (KerML) Version 1.0](https://www.omg.org/spec/KerML/1.0/PDF), formal/2026-03-01, physical page 97. The official PDF is authoritative.
