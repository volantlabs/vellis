---
specification_id: "kerml-1.0"
specification: "Kernel Modeling Language (KerML) Version 1.0"
document_number: "formal/2026-03-01"
source_url: "https://www.omg.org/spec/KerML/1.0/PDF"
source_sha256: "3bcc96f989bfa9d05cd28e026df3351b795fe8d494187b87bff3db7d96373697"
physical_page: 425
printed_page: "399"
generated: true
section_context_before_page:
  - "9 Model Libraries"
  - "9.4 Function Library"
  - "9.4.16 Vector Functions"
  - "9.4.16.2 Elements"
section_paths_starting_here:
  - ["9 Model Libraries", "9.4 Function Library", "9.4.17 Control Functions"]
  - ["9 Model Libraries", "9.4 Function Library", "9.4.17 Control Functions", "9.4.17.1 Control Functions Overview"]
  - ["9 Model Libraries", "9.4 Function Library", "9.4.17 Control Functions", "9.4.17.2 Elements"]
sections_starting_here:
  - "9.4.17 Control Functions"
  - "9.4.17.1 Control Functions Overview"
  - "9.4.17.2 Elements"
extraction_warnings: []
---

# kerml-1.0 physical page 425 (printed 399)

## Extracted specification text

}
function cartesianVectorScalarMult specializes vectorScalarMult {
     in v: CartesianVectorValue[1];
     in x: Real[1];
     return w: CartesianVectorValue[1] = cartesianScalarVectorMult(x, v);
}

function cartesianInner specializes inner {
     in v: CartesianVectorValue[1];
     in w : CartesianVectorValue[1];
     inv precondition { v.dimension == w.dimension }
     return x: Real[1] =
          (1..v.dimension)->collect{in i : Positive; v[i] * w[i]}->reduce RealFunctions::'+';
}

function cartesianNorm specializes norm {
     in v: CartesianVectorValue[1];
     return l : NumericalValue[1] = sqrt(cartesianInner(v, v));
}

function cartesianAngle specializes angle {
     in v: CartesianVectorValue[1]; in w: CartesianVectorValue[1];
     inv precondition { v.dimension == w.dimension }
     return theta: Real[1] = arccos(cartesianInner(v, w) / (norm(v) * norm(w)));
}

function sum {
     in coll: CartesianThreeVectorValue[*];
     return : CartesianThreeVectorValue[1] = sum0(coll, cartesian3DZeroVector);
}

9.4.17  Control Functions
9.4.17.1 Control Functions Overview

This package defines Functions that correspond to operators in the KerML expression notation for which one or
more operands are Expressions whose evaluation is determined by another operand.

9.4.17.2 Elements

abstract function '.' {
     in feature source : Anything[0..*] nonunique {
            abstract feature target : Anything[0..*] nonunique;
     }
     private feature chain chains source.target;
     chain
}

abstract function 'if' {
     in test: Boolean[1];
     in expr thenValue[0..1] { return : Anything[0..*] ordered nonunique; }
     in expr elseValue[0..1] { return : Anything[0..*] ordered nonunique; }
     return : Anything[0..*] ordered nonunique;
}

abstract function '??' {
     in firstValue: Anything[0..*] ordered nonunique;
     in expr secondValue[0..1] { return : Anything[0..*] ordered nonunique; }
     return : Anything[0..*] ordered nonunique;
}

function 'and' {

---

Source: [Kernel Modeling Language (KerML) Version 1.0](https://www.omg.org/spec/KerML/1.0/PDF), formal/2026-03-01, physical page 425. The official PDF is authoritative.
