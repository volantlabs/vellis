---
specification_id: "sysml-2.0"
specification: "OMG Systems Modeling Language (SysML) Version 2.0, Part 1: Language Specification"
document_number: "formal/2026-03-02"
source_url: "https://www.omg.org/spec/SysML/2.0/Language/PDF"
source_sha256: "46e6c0476a6f1f34f367d57e039d56659bff75e41d2e4b3d37ca4cadea84a83a"
physical_page: 376
printed_page: "344"
generated: true
section_context_before_page:
  - "8 Metamodel"
  - "8.3 Abstract Syntax"
  - "8.3.18 States Abstract Syntax"
  - "8.3.18.9 TransitionUsage"
section_paths_starting_here: []
sections_starting_here: []
extraction_warnings: []
---

# sysml-2.0 physical page 376 (printed 344)

## Extracted specification text

     select(kind = TransitionFeatureKind::trigger).transitionFeature->
     selectByKind(Expression)

deriveTransitionUsageSource

The source   of aTransitionUsage      isfeatureTarget     of the result ofsourceFeature(), which must be an
ActionUsage.

source =
     let sourceFeature : Feature = sourceFeature() in
     if sourceFeature = null then null
     else sourceFeature.featureTarget.oclAsType(ActionUsage)

deriveTransitionUsageSuccession

The succession    of a TransitionUsage     is its firstownedMember   that is aSuccession.

succession = ownedMember->selectByKind(Succession)->at(1)

deriveTransitionUsageTarget

The target   of aTransitionUsage      is given by thefeatureTarget     of the targetFeature     of itssuccession,
which must be an  ActionUsage.

target =
     if succession.targetFeature->isEmpty() then null
     else
          let targetFeature : Feature =
                succession.targetFeature->first().featureTarget in
          if not targetFeature.oclIsKindOf(ActionUsage) then null
          else targetFeature.oclAsType(ActionUsage)
          endif
     endif

deriveTransitionUsageTriggerAction

The triggerActions      of aTransitionUsage      are thetransitionFeatures       of the
ownedFeatureMemberships        of the TransitionUsage     with kind = trigger, which must all be
AcceptActionUsages.

triggerAction = ownedFeatureMembership->
     selectByKind(TransitionFeatureMembership)->
     select(kind = TransitionFeatureKind::trigger).transitionFeature->
     selectByKind(AcceptActionUsage)

validateTransitionUsageParameters

A TransitionUsage      must have at least one owned input parameter    and, if it has atriggerAction, it must
have at least two.

if triggerAction->isEmpty() then
     inputParameters()->size() >= 1
else
     inputParameters()->size() >= 2
endif


validateTransitionUsageSuccession


344                                                                             Systems Modeling Language v2.0, Part 1

---

Source: [OMG Systems Modeling Language (SysML) Version 2.0, Part 1: Language Specification](https://www.omg.org/spec/SysML/2.0/Language/PDF), formal/2026-03-02, physical page 376. The official PDF is authoritative.
